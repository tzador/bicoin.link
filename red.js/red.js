const express = require("express");

class Ticker {
  // seconds: number;
  // price: number;
}

class Bet {
  // seconds: number;
  // price: number;
}

class State {
  // seconds: number;
  // history: Array<Ticker> = [];
  // user_bets: Map<string, Bet> = new Map();
}

const app = express();
app.listen(6789);
